# Application
APP_ENV=local
CORS_ORIGINS=http://localhost:3000

# Project database (ORM + Alembic)
APP_DB_HOST=localhost
APP_DB_PORT=5432
APP_DB_NAME=bi_app
APP_DB_USER=postgres
APP_DB_PASSWORD=postgres
# Optional — leave empty to use PostgreSQL default schema (public)
APP_DB_SCHEMA=

# Encrypts user-provided warehouse passwords stored in app.data_sources
CREDENTIALS_SECRET=change-me-to-a-long-random-secret-string

# OpenRouter (AI gateway)
AI_BASE_URL=https://openrouter.ai/api/v1
AI_API_KEY=sk-or-v1-replace-with-your-key
LLM_MODEL=qwen/qwen3-coder:free
LLM_MODEL_FALLBACK=openrouter/free
EMBEDDING_MODEL=nvidia/llama-nemotron-embed-vl-1b-v2:free
EMBEDDING_DIMENSIONS=2048

# Chat / RAG knobs (optional)
RAG_TOP_K=5
SQL_MAX_ATTEMPTS=3
WAREHOUSE_MAX_ROWS=500
CHAT_HISTORY_LIMIT=5

# Warehouse DB credentials are USER INPUT — connect via API (test at /docs after make dev)
# schema_name is optional (PostgreSQL default if empty)
# Demo local warehouse schema: sales (Makefile DEMO_WH_SCHEMA)
# Day 2 flow: POST /api/data/embed-schema → POST /api/chat
